export enum RouterMap {
  HOME_BILL_DETAIL = 'BillDetailPage',
  STATISTICS_BILL_BY_RESOURCE = 'BillByResourcePage',
}

export enum DialogMap {
  COMMON_CONFIRM = 'CommonConfirmDialog',
}

export enum HomeSheetType {
  DATE,
  TYPE,
  CREATE_BILL,
}

export enum BalanceChangeType {
  EXPENSE = 'expense', // 支出
  INCOME = 'income', //收入
}

export enum CustomKeyboardDisplayType {
  TEXT,
  IMAGE,
  TEXT_WITH_IMAGE,
}

export enum CustomKeyboardButtonType {
  NUMBER,
  CALENDAR,
  SUBTRACTION,
  ADDITION,
  POINT,
  DELETE,
  CONFIRM,
}

export enum SummaryType {
  LIST,
  CHART,
}