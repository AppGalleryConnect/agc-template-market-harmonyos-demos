export enum CollageMode {
  FIXED = 'fixed',
  POSTER = 'poster',
  FREE = 'free',
  JOIN = 'join'
}

export enum ActionType {
  TOP_LEFT,
  TOP_RIGHT,
  BOTTOM_LEFT,
  BOTTOM_RIGHT,
  LEFT,
  RIGHT,
  TOP,
  BOTTOM,
  MOVE
}