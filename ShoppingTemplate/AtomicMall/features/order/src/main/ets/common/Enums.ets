export enum OrderTabsMap {
  ALL,
  PENDING_PAYMENT,
  PENDING_SHIPMENT,
  PENDING_RECEIPT,
  REVIEW,
  AFTER_SALE,
}

export enum OrderStatusMap {
  PENDING_PAYMENT = 0,
  CANCELED = 1,
  PENDING_SHIPMENT = 2,
  PENDING_RECEIPT = 3,
  PENDING_REVIEW = 4,
  REVIEWED = 5,
  REFUND_IN_PROGRESS = 6,
  REFUNDED = 7,
  PENDING_PICKUP = 8,
}

export enum OrderButtonStyleMap {
  PRIMARY,
  SECONDARY,
  OTHERS,
}

export enum OrderActionMap {
  IMMEDIATE_PAYMENT,
  URGE_SHIPMENT,
  CONFIRM_RECEIPT,
  CONFIRM_PICKUP,
  CANCEL_ORDER,
  UPDATE_ADDRESS,
  REVIEW,
  VIEW_REVIEW,
  CONTACT_CUSTOMER_SERVICE,
  REQUEST_REFUND,
  CANCEL_REFUND,
  REORDER,
  DELETE,
}

export enum OrderShipmentMap {
  EXPRESS,
  SELF_PICK_UP,
}
