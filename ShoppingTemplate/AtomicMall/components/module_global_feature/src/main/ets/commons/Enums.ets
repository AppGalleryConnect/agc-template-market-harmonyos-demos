export enum LabelPresetType {
  PRIMARY,
  SECONDARY,
}

export enum SortTypeMap {
  DEFAULT,
  PRICE_LOW_TO_HIGH,
  PRICE_HIGH_TO_LOW,
  SALE,
}

export enum ProductOperationType {
  ADD_TO_CART,
  PAY_NOW,
}