/**
 * 窗口持久化
 */
@ObservedV2
export class WindowModels {
  @Trace windowTopPadding: number = 40;
  @Trace windowBottomPadding: number = 28;
  @Trace windowWidth: number = 0;
  @Trace windowHeight: number = 0;
}