import { media } from '@kit.MediaKit';

export class VideoSizeData {
  public photoSize: media.PixelMapParams = {
    width: 0,
    height: 0
  };
  public totalTime: number = 0;
  public rotation: string = '0'
}